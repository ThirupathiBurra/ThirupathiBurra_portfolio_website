import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Experience",
  description: "A track record of building, optimizing, and deploying production-grade AI systems in enterprise and startup environments.",
};

export default function ExperienceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
